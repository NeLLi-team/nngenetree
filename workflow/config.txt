# NNGeneTree Configuration File
# -------------------------

# Input directory containing .faa files
input_dir: example

# BLAST database path
blast_db: /clusterfs/jgi/scratch/science/mgs/nelli/databases/nr/nr

# Number of BLAST hits to retrieve per query
blast_hits: 20

# Number of closest neighbors to extract per query
closest_neighbors: 10

# Query selector (comma-separated list of query prefixes to select specific queries)
query_filter: 

# Taxonomy level for iTOL visualization (domain, phylum, class, order, family, genus, species)
itol_tax_level: class

# Resource configuration for each rule
resources:
  run_diamond_blastp:
    threads: 8
    mem_mb: 32000
    time: "2:00:00"
  align_sequences:
    threads: 8
    mem_mb: 16000
    time: "1:00:00"
    disk_mb: 10000
  build_tree:
    threads: 8
    mem_mb: 16000
    time: "1:00:00"
  extract_hits:
    threads: 1
    mem_mb: 8000
    time: "0:30:00"
